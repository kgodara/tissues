let hour = 3600;

use chrono::{FixedOffset, TimeZone};
let issue_due_date = "2021-05-10"
let timezone = "America/New_York"

// Fetch offset by matching timezone against list of 
let offset: i32 = get_offset_from_timezone() // "-11"

// Get current time in relevant offset

// Current time without UTC offset
let utc: DateTime<Utc> = Utc::now();

// Modify current time with UTC offset



// Due date with UTC offset
let dt = FixedOffset::east(offset*hour).ymd(due_date.year, due_date.month, due_date.day).and_hms(23, 59, 59);

