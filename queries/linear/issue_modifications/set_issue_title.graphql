{   "query": "mutation setIssueTitle($issueId: String!, $ref: String!) {
        issueUpdate(id: $issueId, input: { title: $ref }) {
            success
            issue {
                id
                title
                createdAt
                number
            }
        }
    }",
    "operationName": "setIssueTitle"
}