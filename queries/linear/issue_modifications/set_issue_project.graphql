{   "query": "mutation setIssueProject($issueId: String!, $ref: String!) {
        issueUpdate(id: $issueId, input: { projectId: $ref }) {
            success
            issue {
                id
                title
                createdAt
                number
            }
        }
    }",
    "operationName": "setIssueProject"
}