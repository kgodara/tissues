{   "query": "query fetchWorkflowStatesByTeam($ref: String!) {
        team(id: $ref) {
            states {
                nodes {
                    id
                    name
                    type
                    color
                    description
                }
            }
        }
    }",
    "operationName": "fetchWorkflowStatesByTeam"
}