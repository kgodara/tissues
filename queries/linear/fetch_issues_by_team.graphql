{   "query": "query fetchIssuesByTeam($id: String!, $firstNum: Int) {
        team(id: $id) {
            issues(first: $firstNum) {
                nodes {
                    id
                    createdAt
                    number
                    title
                    description
                    priority
                    estimate
                    state {
                        name
                        type
                        description
                        color
                    }
                    assignee {
                        name
                    }
                }
                pageInfo {
                    hasPreviousPage
                    hasNextPage
                    startCursor
                    endCursor
                }
            }
        }
    }",
    "operationName": "fetchIssuesByTeam"
}